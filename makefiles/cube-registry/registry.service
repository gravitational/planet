[Unit]
Description=Local Docker Registry
Requires=docker.service
After=docker.service

[Service]
ExecStart=/usr/bin/docker run -p 5000:5000 --restart=always --name local-registry -v /ext/registry:/var/lib/registry registry:2
Restart=always
RestartSec=10
