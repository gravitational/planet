package constants

const (
	// KubectlConfigPath is the path to kubectl configuration file
	KubectlConfigPath = "/etc/kubernetes/kubectl.kubeconfig"
	// KubeConfigPath is a privileged kube config
	KubeConfigPath = "/etc/kubernetes/scheduler.kubeconfig"
)
